# Dependencies
node_modules/

# Build
dist/
*.tsbuildinfo

# IDE
.vscode/
.idea/
.codex/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

# Test
coverage/

# Ruff (Python linter cache; matches the entry in .mcpbignore)
.ruff_cache/

# Dream audit workspace (intermediate state from .claude/skills/dream)
# Findings + filtered + manifest are intermediate; review.md / log.md are the
# user-facing audit trail and stay outside git too — easier than splitting.
.dream/

# MCPB build output (produced by scripts/build-mcpb.sh and CI release workflow)
dist-mcpb/
*.mcpb

# Agent Plugins build output (produced by scripts/build-agent-plugin.sh)
dist-agent-plugin/
